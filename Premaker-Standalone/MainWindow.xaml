<?xml version="1.0" encoding="utf-8"?>
<Window  
  x:Class="Premaker.MainWindow"  
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
  xmlns:local="using:Premaker"  
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
  mc:Ignorable="d"  
  Title="Premake Version Manager">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>


    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Premake Versions" Grid.Row="0" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>

        <!-- Version List -->
        <ListView x:Name="ReleasesListView" Grid.Row="1" Margin="0,0,0,12" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:GitHubRelease">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Name}" Grid.Column="0"/>
                        <FontIcon Glyph="&#xE001;"   
                           Grid.Column="1"  
                           VerticalAlignment="Center"  
                           HorizontalAlignment="Right"  
                           Margin="0,0,12,0"  
                           Visibility="{x:Bind IsDownloaded}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Header -->
        <TextBlock Text="Downloads" Grid.Row="2" FontSize="24" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- Package List -->
        <ListView x:Name="PackagesListView" Grid.Row="3" Margin="0,0,0,12" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PremakePackage">
                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind Name}" Grid.Column="0"/>
                        <TextBlock Text="{x:Bind Version}" Grid.Column="1"/>
                        <TextBlock Text="{x:Bind Path}" Grid.Column="2" IsTextSelectionEnabled="True"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Disclaimer -->
        <TextBlock Text="This application is not offically endorsed by Premake" Grid.Row="4" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left"/>

        <!-- Controls -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <ProgressBar x:Name="DownloadProgressBar" Width="100" Height="20" Minimum="0" Maximum="1" Value="0" Margin="0,0,8,0"/>
            <Button Content="Add" Width="80" Click="AddPackage_Click"/>
            <Button Content="Remove" Width="80" Click="RemovePackage_Click"/>
            <Button Content="Refresh" Width="80" Click="Refresh_Click"/>
        </StackPanel>
    </Grid>
</Window>
